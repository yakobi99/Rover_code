Basic git commands:

git status

Staging area:
git add <file name.extension>

Commit to GitHub cloud (w/ message):
git commit -m "message"

Push file to GitHub Cloud:
git push

create new branch:
git checkout -b <branch_name>

switch branches:
git checkout <branch_name>